Fix the docs-lint, docs-prose, and docs-links invoke tasks that referenced README.md, CONTRIBUTING.md, and docs/ relative to the repo root despite tasks.py running from packages/naas. The tasks now wrap their shell-out in 'with c.cd(REPO_ROOT)' so they work from any cwd. Affects nobody who only ran 'invoke docs-check' from the repo root, but the documented 'cd packages/naas; uv run invoke docs-check' workflow now works as advertised.
