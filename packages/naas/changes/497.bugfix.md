Fix two bugs in the post-release CI automation discovered while cutting v2.2.0: (1) the changelog-check workflow now skips release-branch PRs into main since towncrier already consumed all fragments during the release commit, and (2) sync-release.yml now triggers on the release-branch → main PR merge instead of on tag push, so main has the new commits when the main → develop sync PR is opened. The bump-develop job is also gated on this same trigger.
