Rebalance docs/structured-output.md so TextFSM and TTP are presented as the two supported parsers (TextFSM remains primary). Move Platform Autodetect content out of the structured-output page into a new dedicated docs/platform-autodetect.md reference page, added to the Usage section of the docs nav. Fix the deprecated /v1/ URL in the structured-output TTP example. Disable Microsoft.Dashes vale rule because the project consistently uses spaced em-dashes.
