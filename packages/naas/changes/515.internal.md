Pin the Vale prose linter version via a new .mise.toml at the repo root. Contributors install mise (or asdf, or aqua) and run 'mise install' to get the same Vale binary CI uses (vale-action is now pinned to the matching version). Updated CONTRIBUTING.md and docs/development.md with setup instructions and an alternative install path for contributors who'd rather install Vale directly.
