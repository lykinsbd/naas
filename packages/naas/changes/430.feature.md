Add `error_code` and `error_retryable` fields to job results for machine-parseable error classification. Maps each netmiko/paramiko exception to a structured error code (e.g. `CONNECTION_TIMEOUT`, `AUTH_FAILURE`, `CONFIG_REJECTED`). Client library gains exception subclasses routed by error code.
