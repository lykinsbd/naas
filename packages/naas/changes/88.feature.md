Add batch/bulk operations: `POST /v2/send-command/batch` and `POST /v2/send-config/batch` fan out commands to multiple devices in a single API call, `GET /v2/batches/{batch_id}` provides aggregate status. Supports per-device credential overrides, per-device context routing, rate limit integration (cost = N devices), and queue depth checks.
