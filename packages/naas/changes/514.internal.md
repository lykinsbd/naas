Consolidate documentation dependencies into a single 'docs' dependency group at the workspace root. The 'dev' group now includes 'docs' transitively via include-group, so contributors get all five mkdocs plugins with a bare 'uv sync' — no more '--with mkdocs-...' overrides for local doc builds. Read the Docs is switched to native uv install ('method: uv', 'command: sync', 'groups: [docs]') and the duplicated 'docs/requirements.txt' is removed. Single source of truth in pyproject.toml.
